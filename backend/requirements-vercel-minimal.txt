# Minimal requirements for Vercel deployment (without ML features)
# Use this if the full requirements.txt exceeds Vercel's 50MB limit

fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-dotenv==1.0.0
authlib
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
email-validator
alembic==1.12.1
pydantic==2.10.4
pydantic-settings==2.6.1
fastapi-mail==1.5.0

# Note: This excludes ML dependencies (torch, opencv, etc.)
# You'll need to disable or modify AI prediction endpoints if using this

